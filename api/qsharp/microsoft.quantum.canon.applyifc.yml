### YamlMime:QSharpType
uid: microsoft.quantum.canon.applyifc
name: ApplyIfC
type: operation
namespace: Microsoft.Quantum.Canon
summary: >-
  Stosuje operację z możliwością kontrolowania dla klasycznego bitu.


  Dana operacja `op` i wartość `bit`bitowa mają zastosowanie `op` do elementu `target` if `bit` is. `true` Jeśli `false`nie, nic się nie `target`dzieje z.

  Sufiks `C` wskazuje, że operacja do zastosowania jest sterowana.
examples: >-
  Poniżej przygotowuje się rejestr qubits do stanu podstawy obliczeniowej reprezentowanego przez klasyczny ciąg bitowy wyrażony jako tablica `Bool` wartości:


  ```Q#

  let bitstring = [true, false, true];

  using (register = Qubit(3)) {
      ApplyToEach(ApplyIf(X, _, _), Zip(bitstring, register));
      // register should now be in the state |101⟩.
      ...
  }

  ```
syntax: "operation ApplyIfC (op : ('T => Unit is Ctl), bit : Bool, target : 'T) : Unit"
input:
  content: "(op : ('T => Unit is Ctl), bit : Bool, target : 'T)"
  types:
  - name: op
    summary: Operacja, która ma być stosowana warunkowo.
    isOperation: true
    input:
      types:
      - uid: "'T"
        isLocal: true
    output:
      types:
      - isPrimitive: true
        uid: Unit
    functors:
    - Controlled
  - name: bit
    summary: wartość logiczna, która kontroluje, czy operacja jest stosowana, czy nie.
    isPrimitive: true
    uid: Bool
  - name: target
    summary: Dane wejściowe, do których zostanie zastosowana operacja.
    uid: "'T"
    isLocal: true
output:
  content: Unit
  types:
  - isPrimitive: true
    uid: Unit
typeParameters:
- uid: "'T"
  summary: Typ wejściowy operacji, która ma być stosowana warunkowo.
functors:
- Controlled
seeAlso:
- microsoft.quantum.canon.applyifc
- microsoft.quantum.canon.applyifa
- microsoft.quantum.canon.applyifca
metadata:
  ms.openlocfilehash: a41eea8aafeabd5bda6bcc55b958598e58f52184
  ms.sourcegitcommit: db23885adb7ff76cbf8bd1160d401a4f0471e549
  ms.translationtype: MT
  ms.contentlocale: pl-PL
  ms.lasthandoff: 05/01/2020
  ms.locfileid: "82680491"
